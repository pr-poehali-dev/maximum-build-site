# Сводка по развертыванию статической сборки сайта

## Что было проверено

### 1. Конфигурация проекта
- **Файл:** `vite.config.ts`
- **Статус:** Стандартная конфигурация Vite для React
- **Особенности:** 
  - Использует Vite как сборщик
  - React с SWC для быстрой компиляции
  - Алиас `@` для импортов из `./src`
  - Сервер разработки на порту 5173

### 2. Структура приложения
- **Файл:** `src/App.tsx`
- **Роутер:** React Router DOM с BrowserRouter
- **Страницы приложения:**
  - `/` - Главная (Home)
  - `/about` - О компании
  - `/services` - Услуги (с динамическими категориями)
  - `/services/:category` - Категории услуг
  - `/projects` - Портфолио проектов
  - `/projects/:category` - Категории проектов
  - `/portfolio` - Альтернативный путь к портфолио
  - `/stock` - Акции и спецпредложения
  - `/calculator` - Калькулятор стоимости
  - `/team` - Команда
  - `/certificates` - Сертификаты
  - `/contacts` - Контакты
  - `/*` - 404 страница для несуществующих маршрутов

### 3. Скрипты сборки
- **Файл:** `package.json`
- **Доступные команды:**
  - `npm run build` - Production сборка
  - `npm run build:dev` - Development сборка с source maps
  - `npm run preview` - Предпросмотр production сборки
  - `npm run dev` - Запуск dev-сервера

### 4. Существующие сборки
- **Папка:** `builds/ee7bb9ebc280a20f30c7080c936553691e9d63ea/56def/`
- **Статус:** Обнаружена предыдущая сборка
- **Структура:** Стандартная структура Vite build
  - `index.html` с подключенными скриптами
  - `assets/` с минифицированными JS и CSS
  - Статические файлы (favicon.svg, robots.txt)
  - Папка `4x/` с изображениями

## Что было создано

### 1. Файл `.htaccess` для Apache
- **Расположение:** `public/.htaccess`
- **Назначение:** Настройка URL rewriting для React Router
- **Функции:**
  - Перенаправление всех запросов на index.html
  - Включение GZIP сжатия
  - Настройка кэширования статических файлов
- **Будет скопирован в `dist` при сборке**

### 2. Файл `web.config` для IIS
- **Расположение:** `public/web.config`
- **Назначение:** Конфигурация для IIS (если Open Server использует IIS)
- **Функции:**
  - URL rewriting правила
  - MIME types для современных форматов
  - HTTP сжатие
- **Будет скопирован в `dist` при сборке**

### 3. Скрипт автоматической сборки (Windows)
- **Файл:** `build-for-open-server.bat`
- **Назначение:** Автоматизация процесса сборки для Windows
- **Действия:**
  1. Установка зависимостей
  2. Запуск production сборки
  3. Вывод инструкций по развертыванию

### 4. Скрипт автоматической сборки (Unix/Linux/Mac)
- **Файл:** `build-for-open-server.sh`
- **Назначение:** Автоматизация процесса сборки для Unix-систем
- **Действия:** Аналогично Windows версии

### 5. Подробное техническое руководство
- **Файл:** `BUILD_GUIDE.md`
- **Содержание:**
  - Обзор проекта и структуры
  - 3 способа развертывания
  - Конфигурации для Apache и Nginx
  - Решение распространенных проблем
  - Проверочный список
  - Команды для отладки

### 6. Руководство для Open Server
- **Файл:** `OPEN_SERVER_README.md`
- **Содержание:**
  - Пошаговые инструкции для Windows
  - Настройка виртуальных хостов
  - Конфигурация Apache и Nginx
  - Детальное устранение неполадок
  - Контрольный список развертывания

### 7. Этот файл - Сводка
- **Файл:** `DEPLOYMENT_SUMMARY.md`
- **Назначение:** Обзор всех изменений и инструкций

## Как использовать

### Быстрый старт (Рекомендуется)

**Для Windows:**
```bash
# Дважды щелкните по файлу:
build-for-open-server.bat

# Или выполните в командной строке:
npm install
npm run build
```

**Для Unix/Linux/Mac:**
```bash
chmod +x build-for-open-server.sh
./build-for-open-server.sh

# Или:
npm install
npm run build
```

### После сборки

1. Откройте папку `dist`
2. Скопируйте ВСЕ содержимое в папку вашего сайта на Open Server
3. Убедитесь, что `.htaccess` (или `web.config`) присутствует
4. Настройте Apache/Nginx (см. инструкции в `OPEN_SERVER_README.md`)
5. Откройте сайт в браузере

## Важные технические детали

### React Router и BrowserRouter
Проект использует `BrowserRouter`, который создает "чистые" URL без хэшей (`/about` вместо `/#/about`). Это требует серверной настройки:

**Проблема:** При прямом обращении к URL (например, `http://site.com/about`) сервер ищет файл `/about/index.html`, которого не существует.

**Решение:** Файлы `.htaccess` и `web.config` настроены так, чтобы все запросы перенаправлялись на `index.html`, где React Router берет управление на себя.

### Внешние зависимости
Сайт использует внешние CDN:
- **poehali.dev CDN** - скрипты для аналитики и телеметрии
- **Yandex.Metrika** - счетчик посещений

Для работы требуется интернет-подключение.

### Структура после сборки
```
dist/
├── index.html              # Главный HTML файл
├── .htaccess              # Конфигурация Apache
├── web.config             # Конфигурация IIS
├── assets/
│   ├── index-[hash].js    # Минифицированный JavaScript
│   └── index-[hash].css   # Минифицированный CSS
├── favicon.svg
├── placeholder.svg
├── robots.txt
└── 4x/                    # Изображения высокого разрешения
    └── ...
```

### Оптимизации
Сборка включает:
- ✅ Минификация JavaScript и CSS
- ✅ Tree-shaking (удаление неиспользуемого кода)
- ✅ Code splitting
- ✅ Asset optimization
- ✅ GZIP сжатие (через .htaccess)
- ✅ Кэширование статических файлов

## Проверка работоспособности

После развертывания проверьте:

1. **Главная страница**
   - Открывается: `http://your-site.local/`
   - Все элементы загружены
   - Нет ошибок в консоли

2. **Навигация**
   - Клик по ссылкам меню работает
   - URL меняется корректно
   - Страницы загружаются без перезагрузки

3. **Прямые ссылки**
   - `http://your-site.local/about` открывается
   - `http://your-site.local/services` открывается
   - `http://your-site.local/projects` открывается
   - Не появляется 404 ошибка

4. **Статические ресурсы**
   - Изображения отображаются
   - Стили применяются
   - Иконки загружаются

5. **Консоль браузера**
   - Нет красных ошибок
   - Все скрипты загружены (200 OK)
   - CSS файлы загружены

## Что делать при проблемах

### Шаг 1: Проверьте логи Open Server
Правый клик на иконке Open Server → Логи → Apache Error Log (или Nginx Error Log)

### Шаг 2: Проверьте консоль браузера
Нажмите F12 → вкладка Console и Network

### Шаг 3: Проверьте конфигурацию
- Убедитесь, что `.htaccess` находится в корне сайта
- Проверьте, что mod_rewrite включен (для Apache)
- Убедитесь, что AllowOverride разрешен

### Шаг 4: Читайте документацию
- `OPEN_SERVER_README.md` - для базовых проблем
- `BUILD_GUIDE.md` - для технических деталей

## Контакты и поддержка

Все необходимые инструкции находятся в следующих файлах:
- `OPEN_SERVER_README.md` - Главное руководство
- `BUILD_GUIDE.md` - Техническая документация
- `.htaccess` - Конфигурация Apache
- `web.config` - Конфигурация IIS

## Версионность

- **Дата создания:** 18 января 2026
- **Версия проекта:** Из package.json (0.0.0)
- **Версия Node.js:** Рекомендуется 18+ или 20+
- **Версия NPM:** Рекомендуется 9+ или 10+

## Финальный контрольный список

### Перед сборкой:
- [ ] Node.js установлен (версия 18+)
- [ ] NPM или Bun установлен
- [ ] Все зависимости установлены (`npm install`)

### После сборки:
- [ ] Папка `dist` создана
- [ ] Файлы `.htaccess` и `web.config` присутствуют
- [ ] Папка `assets` содержит JS и CSS файлы
- [ ] Статические файлы скопированы

### На сервере:
- [ ] Open Server установлен и запущен
- [ ] Все файлы скопированы в папку сайта
- [ ] Виртуальный хост настроен (если требуется)
- [ ] mod_rewrite включен (для Apache)
- [ ] Сервер перезапущен

### Проверка в браузере:
- [ ] Главная страница открывается
- [ ] Все разделы доступны через меню
- [ ] Прямые ссылки работают
- [ ] Изображения загружаются
- [ ] Нет ошибок в консоли (F12)

---

**Готово к развертыванию!** 🚀

Следуйте инструкциям в `OPEN_SERVER_README.md` для пошагового процесса установки.
